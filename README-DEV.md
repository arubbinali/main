Development notes:

- This project uses Vite + React + TypeScript.
- The `Particles` component uses `ogl` which is already listed as a dependency. You may need to install `react`, `react-dom`, and dev dependencies for Vite.

Install:

```powershell
npm install
``` 

Start dev server:

```powershell
npm run dev
```
